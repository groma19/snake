# Snake

A simple Snake game written in C using [raylib](https://www.raylib.com/).

## Controls

- Move: `Arrow Keys` or `WASD`
- Restart after game over: `Enter`

## Running

```bash
cmake -B build
cmake --build build
./build/snake
```

CMake will automatically fetch and build raylib on the first run.
